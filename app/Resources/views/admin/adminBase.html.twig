<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <meta name="description" content="">
  <meta name="author" content="">

  <link rel="icon" href="../../favicon.ico">
  <title>{% block title %}{{ sitename }}{% endblock %}</title>

  <!-- Bootstrap core CSS -->
  <link href="../../dist/css/bootstrap.min.css" rel="stylesheet">

  {% block stylesheets %}{% endblock %}
  <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
</head>

<body>

{% set route = app.request.attributes.get('_route') %}
<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">

      <ul class="nav navbar-nav">
        <li>
          <a href="{{ path('homepage') }}" class="my-icon"><img src="/assets/images/home.png"></a>
        </li>
        <li {{ route ==  'post_index' ? 'class="active"' }}>
          <a class="navbar-brand" href="{{ path('post_index') }}">The Blogger</a>
        </li>
        <li {{ route ==  'post_index' ? 'class="active"' }}>
          <a class="navbar-brand" href="{{ path('post_index') }}">About</a>
        </li>
      </ul>

      {#<li {{ route ==  'team_index' ? 'class="active"' }}></li>#}
    </div>
    <div id="navbar" class="collapse navbar-collapse">

      <ul class="nav navbar-nav">
        {#<li {{ route ==  'team_index' ? 'class="active"' }}><a href="{{ path('team_index') }}" >Team</a></li>#}
        {#<li {{ route ==  'table' ? 'class="active"' }}><a href="{{ path('table') }}">Table</a></li>#}

        <li {{ route ==  'contact' ? 'class="active"' }}><a href="{{ path('contact') }}">Contact</a></li>

        <ul class="dropdown">
          <h4 class=" dropbtn">Team</h4>

          <li class="dropdown-content">
            <a href="{{ path('team_index') }}">Team</a>
            <a href="{{ path('table') }}">Table</a>
          </li>

        </ul>

        <ul class="dropdown">
          <h4 class=" dropbtn">Games</h4>

          <li class="dropdown-content">
            <a href="{{ path('tennis') }}">Tennis</a>
            <a href="{{ path('snake') }}">Snake</a>
          </li>

        </ul>

      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

<div class="container">

  <div class="row">
    <div class="col-md-12">
      {% block body %}{% endblock %}
    </div>
  </div>

</div>

{% block javascripts %}{% endblock %}

</body>
</html>
