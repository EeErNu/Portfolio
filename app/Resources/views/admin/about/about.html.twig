{% extends 'headerAdmin.html.twig' %}

{% block body %}
  <h1>Companies list</h1>

  <table>
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Location</th>
      <th>Start</th>
      <th>Finish</th>
      <th>Description</th>
      <th>Position</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for company in companies %}
      <tr>
        <td><a href="{{ path('company_show_admin', { 'id': company.id }) }}">{{ company.id }}</a></td>
        <td>{{ company.name }}</td>
        <td>{{ company.location }}</td>
        <td>{% if company.start %}{{ company.start|date('Y-m-d') }}{% endif %}</td>
        <td>{% if company.finish %}{{ company.finish|date('Y-m-d') }}{% endif %}</td>
        <td>{{ company.description }}</td>
        <td>{{ company.position }}</td>
        <td>
          <ul>
            <li>
              <a href="{{ path('company_show_admin', { 'id': company.id }) }}">show</a>
            </li>
            <li>
              <a href="{{ path('company_edit_admin', { 'id': company.id }) }}">edit</a>
            </li>
          </ul>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <ul>
    <li>
      <a href="{{ path('company_new_admin') }}">Create a new company</a>
    </li>
  </ul>

  <h1>Universities list</h1>

  <table>
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Location</th>
      <th>Start</th>
      <th>Finish</th>
      <th>Description</th>
      <th>Field</th>
      <th>Specialization</th>
      <th>Degree</th>
      <th>Grade</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for university in universities %}
      <tr>
        <td><a href="{{ path('university_show_admin', { 'id': university.id }) }}">{{ university.id }}</a></td>
        <td>{{ university.name }}</td>
        <td>{{ university.location }}</td>
        <td>{% if university.start %}{{ university.start|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>{% if university.finish %}{{ university.finish|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>{{ university.description }}</td>
        <td>{{ university.field }}</td>
        <td>{{ university.specialization }}</td>
        <td>{{ university.degree }}</td>
        <td>{{ university.grade }}</td>
        <td>
          <ul>
            <li>
              <a href="{{ path('university_show_admin', { 'id': university.id }) }}">show</a>
            </li>
            <li>
              <a href="{{ path('university_edit_admin', { 'id': university.id }) }}">edit</a>
            </li>
          </ul>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <ul>
    <li>
      <a href="{{ path('university_new_admin') }}">Create a new university</a>
    </li>
  </ul>

  <h1>Volunteers list</h1>

  <table>
    <thead>
    <tr>
      <th>Id</th>
      <th>Organization</th>
      <th>Role</th>
      <th>Cause</th>
      <th>Start</th>
      <th>Finish</th>
      <th>Description</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for volunteer in volunteers %}
      <tr>
        <td><a href="{{ path('volunteer_show_admin', { 'id': volunteer.id }) }}">{{ volunteer.id }}</a></td>
        <td>{{ volunteer.organization }}</td>
        <td>{{ volunteer.role }}</td>
        <td>{{ volunteer.cause }}</td>
        <td>{% if volunteer.start %}{{ volunteer.start|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>{% if volunteer.finish %}{{ volunteer.finish|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>{{ volunteer.description }}</td>
        <td>
          <ul>
            <li>
              <a href="{{ path('volunteer_show_admin', { 'id': volunteer.id }) }}">show</a>
            </li>
            <li>
              <a href="{{ path('volunteer_edit_admin', { 'id': volunteer.id }) }}">edit</a>
            </li>
          </ul>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <ul>
    <li>
      <a href="{{ path('volunteer_new_admin') }}">Create a new volunteer</a>
    </li>
  </ul>
{% endblock %}
