{% extends 'headerAdmin.html.twig' %}

{% block body %}
    <h1>Company creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('company_index_admin') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
